# Automation fixtures

Tests generate ephemeral signing keys and synthetic API responses at runtime. Do not add a real UDID, CMS callback, API key, certificate, or provisioning profile to this directory.
